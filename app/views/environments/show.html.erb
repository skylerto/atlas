<div class="page-header">
  <%= link_to environments_path, class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to edit_environment_path(@environment), class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-pencil"></span>
    Edit
  <% end %>
  <h1><%= @environment.name %> v<%= @environment.version %></h1>
</div>



<h2>Services</h2>
<%= link_to new_service_path, class: 'btn btn-primary' do %>
  <span class="glyphicon glyphicon-plus"></span>
  New Service
<% end %>

<%= render @environment.services %>
